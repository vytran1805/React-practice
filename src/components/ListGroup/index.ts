/**
 * This class is to export ListGroup as a default object so that
 * we don't need to import individual file in the ListGroup folder
 * but the whole folder
 * the import syntax in App.tsx looks cleaner
 */
import ListGroup from "./ListGroup";

// export as a default object
export default ListGroup;
